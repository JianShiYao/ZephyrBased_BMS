# checkpatch 配置 —— 从 zephyr/.checkpatch.conf 沿用,供 app 仓库使用。
# 说明:
#   - checkpatch.pl 本体来自 zephyr(check_compliance.py 用 $ZEPHYR_BASE/scripts/checkpatch.pl),
#     本文件只提供参数;checkpatch 以「当前工作目录」查找 .checkpatch.conf,
#     故必须放在 app 仓库根目录。
#   - 已移除上游的 `--typedefsfile=scripts/checkpatch/typedefsfile`:该路径相对 CWD(app 根),
#     在 app 仓库里不存在;它是 Zephyr 内部 typedef 清单,应用代码无需依赖。
# 升级策略:zephyr 版本升级时,对照上游 .checkpatch.conf 的 --ignore 列表增量同步。

--emacs
--summary-file
--show-types
--max-line-length=100
--min-conf-desc-length=1

--ignore PRINTK_WITHOUT_KERN_LEVEL
--ignore SPLIT_STRING
--ignore VOLATILE
--ignore CONFIG_EXPERIMENTAL
--ignore PREFER_KERNEL_TYPES
--ignore PREFER_SECTION
--ignore AVOID_EXTERNS
--ignore NETWORKING_BLOCK_COMMENT_STYLE
--ignore DATE_TIME
--ignore MINMAX
--ignore CONST_STRUCT
--ignore FILE_PATH_CHANGES
--ignore SPDX_LICENSE_TAG
--ignore C99_COMMENT_TOLERANCE
--ignore REPEATED_WORD
--ignore UNDOCUMENTED_DT_STRING
--ignore DT_SPLIT_BINDING_PATCH
--ignore DT_SCHEMA_BINDING_PATCH
--ignore TRAILING_SEMICOLON
--ignore COMPLEX_MACRO
--ignore MULTISTATEMENT_MACRO_USE_DO_WHILE
--ignore ENOSYS
--ignore IS_ENABLED_CONFIG
--ignore EXPORT_SYMBOL
--ignore COMPARISON_TO_NULL
