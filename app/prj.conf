# 最小点灯应用配置(尽量精简以适配 64KB 内部 Flash)
CONFIG_GPIO=y
CONFIG_PRINTK=y

# 日志子系统:双后端(UART 串口 + SEGGER RTT),供调试器与串口同时看日志
CONFIG_LOG=y
CONFIG_USE_SEGGER_RTT=y
CONFIG_LOG_BACKEND_RTT=y
CONFIG_LOG_BACKEND_UART=y
